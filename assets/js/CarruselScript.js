$(document).ready(function() {
    $("#carousel-inicio").carousel({
        interval: 20, // Cambia de imagen cada 3 segundos
        pause: "hover"  // Se pausa cuando el mouse está sobre él
    });
});
